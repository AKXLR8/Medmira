version: "3.8"
services:
  mongo:
    image: mongo
    container_name: mongo
    ports:
      - "27017:27017"

  app:
    build: .
    container_name: prescription-app
    ports:
      - "8080:8080"
    environment:
      - MONGO_URI=mongodb+srv://akshaymaniyampara_db:JQWtOJYss2saUL2L@cluster0.7tpjgzs.mongodb.net/?appName=Cluster0
    depends_on:
      - mongo